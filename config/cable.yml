development:
  adapter: redis
  url: <%= ENV.fetch "REDIS_URL" %>
  channel_prefix: code_review_development

test:
  adapter: redis
  url: <%= ENV.fetch "REDIS_URL" %>
  channel_prefix: code_review_test

production:
  adapter: redis
  url: <%= ENV.fetch "REDIS_URL" %>
  channel_prefix: code_review_production
